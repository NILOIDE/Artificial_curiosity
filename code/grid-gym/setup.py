#!/usr/bin/env python
# -*- coding: utf-8 -*-

from setuptools import setup, find_packages


setup(name='GridWorld10x10',
      version='0.1',
      install_requires=['gym', 'numpy'],
      packages=find_packages()
      )

setup(name='GridWorld25x25',
      version='0.1',
      install_requires=['gym', 'numpy'],
      packages=find_packages()
      )

setup(name='GridWorld42x42',
      version='0.1',
      install_requires=['gym', 'numpy'],
      packages=find_packages()
      )

setup(name='GridWorldRandFeatures42x42',
      version='0.1',
      install_requires=['gym', 'numpy'],
      packages=find_packages()
      )

setup(name='GridWorldSubspace50x50',
      version='0.1',
      install_requires=['gym', 'numpy'],
      packages=find_packages()
      )

setup(name='GridWorldBox11x11',
      version='0.1',
      install_requires=['gym', 'numpy'],
      packages=find_packages()
      )

setup(name='GridWorldSpiral28x28',
      version='0.1',
      install_requires=['gym', 'numpy'],
      packages=find_packages()
      )

setup(name='GridWorldSpiral52x50',
      version='0.1',
      install_requires=['gym', 'numpy'],
      packages=find_packages()
      )


